{
  "name": "project-v-authentication-database",
  "test:unit": "NODE_OPTIONS=--experimental-vm-modules jest tests/unit --runInBand",
  "test:integration": "NODE_OPTIONS=--experimental-vm-modules jest tests/integration --runInBand",
  "test:regression": "NODE_OPTIONS=--experimental-vm-modules jest tests/regression --runInBand",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.75.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}